plugin: amazon.aws.aws_ec2
regions:
  - ca-central-1
filters:
  tag:Name:
    - web-asg-instance
  instance-state-name:
    - running
keyed_groups:
  - key: tags.Name
    prefix: tag
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address
